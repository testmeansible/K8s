apiVersion: apps/v1
kind: Deployment
metadata:
  name: override-deployment-ctf
  labels:
    app: override-ctf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: override-ctf
  template:
    metadata:
      labels:
        app: override-ctf
    spec:
      # runtimeClassName: gvisor  # Secure runtime 
      # restartPolicy: Always  # Explicitly set
      containers:
        - name: override
          image: abeerfatima/access_override:v3
          # securityContext:
          #   # allowPrivilegeEscalation: false  # Prevent privilege escalation attacks
          #   # readOnlyRootFilesystem: true  # Ensures root filesystem is immutable
          #   seccompProfile:
          #     type: RuntimeDefault  # Uses default syscall restrictions
          resources:
            limits:
              cpu: "1"
              memory: "512Mi"
              ephemeral-storage: "1Gi"
            requests:
              cpu: "500m"
              memory: "256Mi"
              ephemeral-storage: "500Mi"
