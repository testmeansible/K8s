apiVersion: v1
kind: Pod
metadata:
  name: ecc-backdoor-sandbox-pod
  # namespace: it  # Update or remove based on your setup
  labels:
    app: ecc-backdoor-sandbox
spec:
  restartPolicy: Always
  containers:
    - name: ecc-backdoor-container
      image: '192.168.3.46:5000/ecc_backdoor:latest'
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
        # allowPrivilegeEscalation: true
        # readOnlyRootFilesystem: false
        seccompProfile:
          type: RuntimeDefault
      resources:
        limits:
          cpu: "1"
          memory: "512Mi"
          ephemeral-storage: "5Gi"
        requests:
          cpu: "0.5"
          memory: "256Mi"
          ephemeral-storage: "2Gi"